@model IEnumerable<personapi_dotnet.Models.Entities.estudio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.univer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cc_perNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_profNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.univer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cc_perNavigation.cc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_profNavigation.id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
